# Convenience wrapper — delegates to the real CLI entry points.
# Usage:
#   python -m src.main <command>         (cyber threat pipeline)
#   python -m src.aiml_main <command>    (ML/AI landscape pipeline)
